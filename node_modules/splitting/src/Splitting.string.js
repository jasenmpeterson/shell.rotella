// Splitting.string = function(str) {
//   var el = document.createElement("span");
//   el.innerHTML = str;
//   var s = this(el)[0];
//   return s.el.outerHTML;
// };

// /**
//  * # Splitting.chars.string
//  * Splits a string by chars and returns the processed HTML with elements and
//  * CSS Vars.
//  */
// Splitting.chars.string = Splitting.string.bind(Splitting.chars);
// Splitting.words.string = Splitting.string.bind(Splitting.words);

// Splitting.string('words', 'my string')
// Splitting.string.words('my string')
// Splitting.string('my string', { type: 'words' });
// Splitting.words.string('my string')
// Splitting.chars.toString('my string');
// Splitting.fromString('my string', { type: 'words' });
